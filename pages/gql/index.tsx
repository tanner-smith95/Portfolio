import { ApolloSandbox } from '@apollo/sandbox/react';

export default function GraphQLSandbox() {

    return (
        <div className='gql-sandbox'>
            <ApolloSandbox />
        </div>
    );
}